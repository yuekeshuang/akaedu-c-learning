#include <stdio.h>
#define ECHO(s)    gets(s), putchar(10), puts(s)
//#define DEBUG 1
int  main(void)
{
  char s[128];
  int flag = 0;
  else	
	putchar('N');
#endif
  /* #ifdef 
	 if(flag == 1) 
	 ECHO(s);
#endif
#elif 
if(flag == 1) 
ECHO(s);
#endif
   */
  printf("hello\n");
  /*do {
	ECHO(s)	;
	ECHO(s);
	}while(0);  */

  return 0;
}

