#include<stdio.h>
int add(int x,int y)
{
int a;
a=x+y;
return (a);

}
int add(int,int);
int main()
{
int a;
a=add(5,2);
printf("%d\n",a);
return 0;



}


                                                    1. 字符串查找，判断里面是否有‘h'。
2. 字符串遍历，判断是否有叠字出现，统计出现的叠字为哪些。
                                                    3. 测试一个字符串里第一个出现’ ‘或’\0‘的位置。
4. 五子棋程序，判断某一坐标位置是否可以落棋子。
                                                    5. 计算两个数的平均数，可以是浮点类型。
6. 查找替换字符串里的某个单词，
如用“hello”替换掉字符串中所有的“world”。
                                                    7. 判断一个字符串里是否出现了n个'x'。
+
