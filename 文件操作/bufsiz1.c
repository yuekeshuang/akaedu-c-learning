#include <stdio.h>
#include <unistd.h>

int main(void)
{
  FILE *fp_r, *fp_w;
  fp_r = fopen("abc", "r+");
  fp_r - fopen("abc", "r");
  fwrite(buf, 1, strlen(buf), fp_w);
  len = fread(r_buf, 1, sizeof(r_buf),fp_r);
  fwrite();
	
}
